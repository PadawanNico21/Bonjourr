export enum Size {
    ExtrasSmall,
    Small,
    Medium,
    ExtrasMedium,
    Large,
    ExtrasLarge,
}

export enum Region {
    Europe = 'eu',
    NorthAmerica = 'na',
    SouthAmerica = 'sa',
    Asia = 'as',
    Africa = 'af',
    Oceania = 'oc',
    Antarctica = 'an',
}

export enum Unit {
    Metric,
    Imperial,
}

export enum ServerType {
    RemoteWithAuthentication,
    RemoteWithStaticFile,
    LocalExtension,
    LocalPage,
}

export enum SearchAutosuggestionProvider {
    Google,
}

export enum Theme {
    System,
    Light,
    Dark,
}
